
component extends="tests.specs.unit.BaseModelTest" model="models.DomainDNS"{

	function run(){

		describe( "DomainDNS Suite", function(){

			it( "should setDefault", function(){
				expect( false ).toBeTrue();
			});

			it( "should setCustom", function(){
				expect( false ).toBeTrue();
			});

			it( "should getList", function(){
				expect( false ).toBeTrue();
			});

			it( "should getHosts", function(){
				expect( false ).toBeTrue();
			});

			it( "should getEmailForwarding", function(){
				expect( false ).toBeTrue();
			});

			it( "should setEmailForwarding", function(){
				expect( false ).toBeTrue();
			});

			it( "should setHosts", function(){
				expect( false ).toBeTrue();
			});


		});

	}

}
